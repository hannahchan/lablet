# --manifest-path resolves against the current directory, so this alias works
# from the repository root only. lablet/.cargo/config.toml carries a copy for
# the workspace root; keep the two in step.
[alias]
xtask = "run -q --locked --manifest-path xtask/Cargo.toml --"
