// Generated by `cargo xtask weaver generate` from lablet/telemetry/registry/; don't edit.

//! The closed value sets: one enum for each lablet attribute whose type is a
//! list of members.

{% for attr in ctx %}
{{ [attr.brief, "\n\nThe values of `" ~ attr.key ~ "`."] | comment }}
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum {{ attr.key | pascal_case }} {
{% for member in attr.type.members %}
    {{ member.brief | comment(indent=4) }}
    {{ member.id | pascal_case }},
{% endfor %}
}

impl {{ attr.key | pascal_case }} {
    /// The value as it appears on the wire.
    #[must_use]
    pub const fn as_str(self) -> &'static str {
        match self {
{% for member in attr.type.members %}
            Self::{{ member.id | pascal_case }} => "{{ member.value }}",
{% endfor %}
        }
    }
}

impl core::fmt::Display for {{ attr.key | pascal_case }} {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.as_str())
    }
}

{% endfor %}
