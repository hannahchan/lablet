// Generated by `cargo xtask weaver generate` from lablet/telemetry/registry/; don't edit.

//! Attribute keys: lablet's own, and every semantic-convention attribute that
//! a lablet span, event, or resource carries.

{% for attr in ctx %}
{% if attr.type is string and attr.type is startingwith("template[") %}
{{ [attr.brief, "\n\nA template: the emitted key is this prefix, a dot, and `<key>`."] | comment }}
{% else %}
{{ attr.brief | comment }}
{% endif %}
pub const {{ attr.key | screaming_snake_case }}: &str = "{{ attr.key }}";

{% endfor %}
