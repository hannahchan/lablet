//! Test support: a tiny `rmcp` stdio server whose tools echo, sleep, and make
//! the server exit after N calls, for the `lablet-tools-mcp` tests.
//!
//! An empty shell from the phase 0 scaffold; build-plan phase 8 fills it.

/// Does nothing yet; the server arrives in build-plan phase 8.
fn main() {}
