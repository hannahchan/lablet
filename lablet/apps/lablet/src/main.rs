//! The `lablet` command line: `init`, `run`, `check`, and `schema` over the
//! library in this package.
//!
//! An empty shell from the phase 0 scaffold; build-plan phase 5 fills it.

/// Does nothing yet; the CLI arrives in build-plan phase 5.
fn main() {}
