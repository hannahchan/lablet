//! The composition root as a library: config types, `build()`, and the `Lablet`
//! handle that runs the loop. It is the one place that selects adapters.
//!
//! An empty shell from the phase 0 scaffold; build-plan phase 4 fills it.
