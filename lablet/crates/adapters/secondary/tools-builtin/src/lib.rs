//! Secondary adapter: a `ToolExecutor` for `bash`, `read_file`, and `write_file`
//! (`task_complete` lives in the loop, not here).
//!
//! An empty shell from the phase 0 scaffold; build-plan phase 4 fills it.
