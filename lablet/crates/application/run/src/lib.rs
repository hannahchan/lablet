//! The application ring: `RunService` (the agent loop) and the secondary ports
//! it consumes (`ModelProvider`, `ToolExecutor`, `RunObserver`).
//!
//! An empty shell from the phase 0 scaffold; build-plan phase 3 fills it.
