# A copy of the alias in ../.cargo/config.toml with the path adjusted, because
# --manifest-path resolves against the current directory. Keep the two in step.
[alias]
xtask = "run -q --locked --manifest-path ../xtask/Cargo.toml --"
